---
title: Home page
lead:
---

<%= partial "includes/header" %>

<%= partial "includes/nav" %>

<div class="container">
  <div class="row justify-content-center py-4">
    <div class="col-12 col-md-6">
      <p class="lead">One short sentence here about why this is happening and what goes on once blah blah blah</p>
    </div>
  </div>
  <div class="row">
  <% data.companies.each_with_index do |c,i| %>
    <div class="col-12 col-md-4 business-card">
      <div class="logo">
        <img class="img-fluid" src="images/logos/<%= c.logo %>" alt="<%= c.name %> logo">
      </div>
      <p class="my-4"><%= c.summery %></p>
      <a href="<%= c.url %>" class="btn btn-primary">Learn more</a>
    </div>
    <% if (i + 1) % 3 == 0 %>
  </div><div class="row">
    <% end %>
  <% end %>
  </div>
</div> <!-- /container -->

<%= partial "includes/footer" %>
